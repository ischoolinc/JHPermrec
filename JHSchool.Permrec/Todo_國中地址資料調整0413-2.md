# Cursor Todo.md
目標：安全修正 AddressPalmerwormItem 的取消按鈕行為。  
要求：採用較保守寫法，按下取消時要將畫面還原成 `_StudAddressRec` 目前資料，避免只隱藏按鈕卻保留未儲存內容。  
完成後記錄在：國中地址資料調整0413.md

---

## 一、問題摘要

目前 `AddressPalmerwormItem.cs` 的取消按鈕：

```csharp
protected override void OnCancelButtonClick(EventArgs e)
{
    SaveButtonVisible = false;
    CancelButtonVisible = false;
}
````

這樣只會把按鈕隱藏，不會把畫面欄位還原。

造成問題：

1. 使用者修改地址後按取消
2. 畫面仍保留修改後內容
3. 但資料沒有儲存
4. 使用者誤以為已取消成功

=> 屬於「假取消」問題

---

## 二、修改目標

* 按取消時，畫面恢復 `_StudAddressRec` 原始資料
* 清除所有未儲存變更
* 不影響既有邏輯（ZipCode / County / Town / Save）
* 使用「保守修改」，只動取消邏輯

---

## 三、指定修改檔案

只修改：

* AddressPalmerwormItem.cs

不要修改：

* AddressPalmerwormItem.designer.cs

---

## 四、修改方式（保守寫法）

### 修改 OnCancelButtonClick

將原本：

```csharp
protected override void OnCancelButtonClick(EventArgs e)
{
    SaveButtonVisible = false;
    CancelButtonVisible = false;
}
```

改為：

```csharp
protected override void OnCancelButtonClick(EventArgs e)
{
    BindDataToForm();
    SaveButtonVisible = false;
    CancelButtonVisible = false;
}
```

---

## 五、設計說明（為什麼這樣改）

* `BindDataToForm()`：

  * 會把 `_StudAddressRec` 的資料重新載回畫面
  * 已有 `_date_updating` 保護，不會觸發 zipcode 覆寫問題
  * 會 Reset ChangeListener
  * 會讓畫面回到乾淨狀態

* 最後再顯式設定：

  ```csharp
  SaveButtonVisible = false;
  CancelButtonVisible = false;
  ```

  => 保守做法，確保 UI 狀態一致

---

## 六、不可修改項目（重要）

請不要改動以下邏輯：

* OnSaveButtonClick()
* BindDataToForm() 主流程
* CheckTownChange()
* CheckZipCode()
* cboTown_TextChanged()
* txtZipcode_KeyDown()
* txtZipcode_Validated()

避免影響既有資料流

---

## 七、驗證測試

### 測試 1：取消還原

操作：

* 修改 ZipCode / County / Town / Detail
* 按取消

預期：

* 全部欄位回復原值

---

### 測試 2：取消不寫入資料

操作：

* 修改資料 → 按取消 → 重新載入

預期：

* DB 未變

---

### 測試 3：按鈕狀態

預期：

* SaveButtonVisible = false
* CancelButtonVisible = false

---

### 測試 4：Town → ZipCode

操作：

* 手動改 cboTown

預期：

* txtZipcode 仍會同步更新

---

### 測試 5：ZipCode → Town

操作：

* 輸入 ZipCode → Enter

預期：

* County / Town 正確反查

---

### 測試 6：搭配 0413 zipcode 修正

操作：

* 載入 ZipCode ≠ Town mapping 的資料
* 修改 → 按取消

預期：

* 回復原始 ZipCode
* 不會被覆寫

---

## 八、修改完成紀錄

請新增或更新：

國中地址資料調整0413.md

內容包含：

### 問題

取消按鈕只隱藏 UI，不會還原資料

### 原因

OnCancelButtonClick 未重新載入 `_StudAddressRec`

### 修正

改為：

```csharp
BindDataToForm();
```

並保留：

```csharp
SaveButtonVisible = false;
CancelButtonVisible = false;
```

### 結果

* 取消可正確還原
* 不影響原有功能
* UI 行為正確

---

## 九、提醒

* 本次為「安全修正」
* 不重構、不優化其他邏輯
* 不新增新變數
* 不動資料層
* 不動 Designer
